Need a page that verifies that the user has sucessfully signed up.

Finish user upload image function
Send to editor view

Gallery pagination display

Adding image overlays
Verify wether email actually exists.

need to add content to editor, account settings, and gallery pages

need to make editor page in nav bar but only visible when logged in. same with account settings. --> DONE - please test it works

one gallery php file with commenst permissions changing? or two files, one with allowed(logged in only) and one with not allowed to comment (public)?? not sure which is easier to do.

need to make a checkbox type system to choose an image from preset list to add touploaded image.
